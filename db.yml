docker run --name mi-mysql \
  -e MYSQL_ROOT_PASSWORD=toor \
  -e MYSQL_DATABASE=chillsage_db \
  -e MYSQL_USER=chillsage \
  -e MYSQL_PASSWORD=chillsage \
  -p 3307:3306 \
  -v mysql_data:/var/lib/mysql \
  -d mysql:latest